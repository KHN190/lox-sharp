fun count(n) 
{
  if (n > 1) count(n - 1);
  print n;
}

print count;

// native function `clock`
var start = clock();

count(5);

var end = clock();

print "using time: " + (end - start) + " s.";

var f = fun(n) { return n + 1; }

print f;

print f(1); // 2
